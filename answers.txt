2.a) Если переменная "a" до этой строчки кода не была создана в window, то будет вывод undefined в alert
	 т.к. if образует область видимости и к моменту вывода "a" её не будет. Если "a" в "window" уже
	 существует, то будет произведён вывод значения "а"
	
2.b) Ошибка исполнения т.к. "a" имеет область видимости только внутри функции

2.с) Вывод кода функции в alert, т.к. не производится вызов функции а передача по ссылке в alert

2.d) вывод значения 10, т.к. в теле функции аргументу №2 (начиная с 0) присваивается 10.

2.e) window, т.к. фактически производится вызов функции без контекста, в этом случае this имеет значение window